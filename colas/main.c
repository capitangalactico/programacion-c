#include <stdio.h>
#include <stdlib.h>
#include "cola.h"

int main(void) {
  Cola cola;
  cola.frente= cola.atras = size-1;
  /*
  printf("Empty %d\n", empty(&pila));
  push(&pila, 1);
  printf("Pop %d\n", pop(&pila));
  printf("Empty %d\n", empty(&pila));*/
  return 0;
}
